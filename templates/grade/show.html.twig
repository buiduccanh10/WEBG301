{% extends 'base.html.twig' %}

{% block title %}Grade{% endblock %}

{% block body %}
    <h1>Grade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grade.id }}</td>
            </tr>
            <tr>
                <th>Grade_id</th>
                <td>{{ grade.gradeId }}</td>
            </tr>
            <tr>
                <th>Grade_result</th>
                <td>{{ grade.gradeResult }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_grade_index') }}">back to list</a>

    <a href="{{ path('app_grade_edit', {'id': grade.id}) }}">edit</a>

    {{ include('grade/_delete_form.html.twig') }}
{% endblock %}
